{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "core access for the manager window, all pet windows and the usage gauge",
  "windows": ["manager", "pet-*", "usage-gauge"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
